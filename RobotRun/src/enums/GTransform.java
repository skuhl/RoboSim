package enums;

/**
 * Defines the different operations for applying transformations to a PGraphics
 * object.
 * 
 * @author Joshua Hooker
 */
public enum GTransform {
	RESET, TRANS, ROTATE_X, ROTATE_Y, ROTATE_Z, R_MATRIX, T_MATRIX;
}
