package enums;

/**
 * Defines the states of program execution.
 * 
 * @author Joshua Hooker
 */
public enum ExecState {
	EXEC_CALL, EXEC_DONE, EXEC_FAULT, EXEC_INST, EXEC_MINST, EXEC_NEXT,
	EXEC_START;
}
